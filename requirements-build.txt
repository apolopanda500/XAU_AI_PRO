# XAU_AI_PRO - Dependencias para BUILD do EXE (PyInstaller) e CI
# Enxuto GUI-ONLY: apenas o que o launcher.spec realmente embute.
# (requirements.txt completo continua valido para desenvolvimento)
# NOTA: Streamlit/Altair/Uvicorn/OpenAI foram retirados do build
# (dashboard web nao faz parte do EXE; desktop usa so a GUI nativa).
numpy
pandas
scikit-learn
scipy
joblib
sentry-sdk[openai]>=2.44.0
Pillow
MetaTrader5
pyinstaller