[package]
name = "xau-ai-pro-desktop"
version = "0.1.0"
edition = "2021"

[build-dependencies]
tauri-build = { version = "1.5.2", features = [] }

[features]
custom-protocol = ["tauri/custom-protocol"]

[dependencies]
tauri = { version = "1.5.4", features = ["api-all", "process-command-api"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
dirs = "5"


