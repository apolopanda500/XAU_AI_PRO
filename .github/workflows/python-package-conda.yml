name: Python Package using Conda

on:
  push:
    branches: [ main ]
    paths:
      - 'requirements-ci.txt'
      - 'requirements-dev.txt'
      - 'requirements.txt'
      - 'pyproject.toml'
      - '.github/workflows/python-package-conda.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'requirements-ci.txt'
      - 'requirements-dev.txt'
      - 'requirements.txt'
      - 'pyproject.toml'
      - '.github/workflows/python-package-conda.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-linux:
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 5

    steps:
    - uses: actions/checkout@v4
    - name: Set up Miniconda
      uses: conda-incubator/setup-miniconda@v3
      with:
        auto-update-conda: true
        python-version: '3.11'
        channels: conda-forge,defaults
        channel-priority: strict
    - name: Install dependencies
      shell: bash -el {0}
      run: |
        conda --version
        python --version
        pip install --upgrade pip
        if [ -f requirements-ci.txt ]; then
          pip install -r requirements-ci.txt
        fi
    - name: Lint with flake8
      shell: bash -el {0}
      run: |
        conda install -y flake8
        # stop the build if there are Python syntax errors or undefined names
        # F821 (undefined name) se omite: produce falsos positivos por imports
        # dinamicos/globals de modulo en este codigo.
        flake8 . --count --select=E9,F63,F7,F82 --extend-ignore=F821 --show-source --statistics
        # exit-zero treats all errors as warnings. The GitHub editor is 127 chars wide
        flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    - name: Test with pytest
      shell: bash -el {0}
      run: |
        # Los tests usan tkinter (GUI), que requiere un display X.
        # En CI headless se usa xvfb (X Virtual Framebuffer).
        command -v xvfb-run >/dev/null 2>&1 || sudo apt-get update && sudo apt-get install -y xvfb
        xvfb-run -a python -m pytest -q tests
