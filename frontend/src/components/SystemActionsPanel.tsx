import { useAppStore } from '../hooks/useAppStore';
export default function SystemActionsPanel(){const setTab=useAppStore(s=>s.setActiveTab);return <div className="card" style={{marginTop:14}}><div className="section-head"><div><h2>Comandos do sistema</h2><span className="muted">Ações disponíveis e seguras</span></div><span className="chip warn">Reinício protegido</span></div><div className="btn-row"><button className="btn primary" onClick={()=>window.dispatchEvent(new Event('xau-refresh'))}>Atualizar leituras</button><button className="btn ghost" onClick={()=>setTab('portfolio')}>Abrir patrimônio</button><button className="btn ghost" onClick={()=>setTab('robot')}>Abrir operações</button><button className="btn ghost" onClick={()=>setTab('settings')}>Configurar conexões</button><button className="btn danger" disabled title="Endpoint de emergência ainda não validado">Emergência indisponível</button></div><div className="hint">Nenhum comando de reinício, desligamento ou negociação é executado sem endpoint seguro validado.</div></div>}
